127.0.0.1       <%= @fqdn_entry %> <%= @hostname_entry %> <% @localhost_entries.each do |localhost_entries | -%>  <%= localhost_entries %> <% end -%>  localhost localhost.localdomain localhost4 localhost4.localdomain4
::1     	<%= @fqdn_entry %> <%= @hostname_entry %> <% @localhost_entries.each do |localhost_entries | -%>  <%= localhost_entries %> <% end -%> localhost localhost.localdomain localhost6 localhost6.localdomain6

